# Ignore failures when optional dependencies cannot be installed
# (e.g. @circle-fin/* packages require Circle Console access)
# The app uses dynamic imports and stubs when these are absent.
legacy-peer-deps=true
fund=false
audit=false
